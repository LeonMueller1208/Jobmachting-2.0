{
  "buildCommand": "cd web && npx prisma generate && npx prisma db push && npm run build",
  "outputDirectory": "web/.next",
  "installCommand": "cd web && npm install",
  "framework": "nextjs",
  "functions": {
    "web/app/api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  }
}
